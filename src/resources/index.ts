/**
 * Resource registry.
 * Resources are added alongside their corresponding tool families.
 */

import type { ResourceDefinition } from "../tools/types.js";

export const resources: ResourceDefinition[] = [];
